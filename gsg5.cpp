#include<iostream>
using namespace std;
string sortsting(string s,int n)
{
    for(int i=0;i<n;i++)
    {
        for(int j=0;j<n;j++)
        {
            if(s[i]-)
        }    
    }
    return s;
}
int main(void)
{
    string s;
    cin>>s;
    int n=s.size();
    cout<<sortsting(s,n);
    return 0;
}